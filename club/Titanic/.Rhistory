setwd("~/school/club/Titanic")
titanicRaw <- read.table("test.csv", sep="," , header = TRUE)
titanicRaw <- read.table("train.csv", sep="," , header = TRUE)
titanicRaw$Age
avgAge <- mean(titanicRaw$age, na.rm = TRUE)
avgAge
avgAge <- mean(titanicRaw$Age, na.rm = TRUE)
avgAge
cleanAge <- numeric(891)
for (i in 1:891){}
for (i in 1:891){
if(is.na(titanicRaw$Age[i])){
cleanAge[i] <- avgAge
} else {}
}
cleanAge
